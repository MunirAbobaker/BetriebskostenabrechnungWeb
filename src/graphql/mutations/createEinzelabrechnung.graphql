
mutation createEinzelabrechnung( 
   $options: [EinzelabrechnungType!]!
) {
   createEinzelabrechnung(
      options: $options
  )  {
        errors {
      ...RegularError
    }

     Einzelabrechnungsdata {
      Abrechnungsposition
    }
   
  }
}
